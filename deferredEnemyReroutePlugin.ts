import type { Plugin } from "vite";

/**
 * Keeps enemy rerouting safe after plant placement/removal.
 * Enemies never teleport during a reroute and never receive an empty path.
 */
export function deferredEnemyReroute(): Plugin {
  return {
    name: "deferred-enemy-reroute",
    enforce: "pre",
    transform(code, id) {
      const normalizedId = id.split("?")[0].replace(/\\/g, "/");
      if (!normalizedId.endsWith("/src/main.ts")) return null;

      const fieldAnchor = "  private pathRecalculationVersion = 0;";
      const fieldReplacement = `  private pathRecalculationVersion = 0;\n  private pendingEnemyReroutes = new WeakSet<Enemy>();`;
      if (!code.includes(fieldAnchor)) {
        throw new Error("Deferred reroute field anchor not found.");
      }

      const followPathAnchor = `  private followPath(enemy: Enemy, delta: number, speed: number): void {\n    const target = enemy.path[enemy.pathIndex];\n    if (!target) {\n      this.recalculateEnemyPath(enemy);\n      return;\n    }\n\n    const distance = Phaser.Math.Distance.Between(enemy.body.x, enemy.body.y, target.x, target.y);\n    const step = speed * (delta / 1000);\n    if (distance <= step) {\n      enemy.body.setPosition(target.x, target.y);\n      enemy.pathIndex += 1;\n      return;\n    }\n    enemy.body.x += ((target.x - enemy.body.x) / distance) * step;\n    enemy.body.y += ((target.y - enemy.body.y) / distance) * step;\n  }`;

      const followPathReplacement = `  private rerouteEnemySafely(enemy: Enemy): boolean {\n    const halfPlant = PLANT_FRAME_SIZE / 2 - 1;\n    const rightExit = {\n      id: "right" as ExitId,\n      col: TOP_EXIT_COL,\n      row: TOP_EXIT_ROW,\n      x: this.gridToWorldX(TOP_EXIT_COL, TOP_EXIT_ROW),\n      y: this.gridToWorldY(TOP_EXIT_ROW),\n    };\n    const bottomExit = {\n      id: "bottom" as ExitId,\n      col: BOTTOM_EXIT_COL,\n      row: BOTTOM_EXIT_ROW,\n      x: this.gridToWorldX(BOTTOM_EXIT_COL, BOTTOM_EXIT_ROW),\n      y: this.gridToWorldY(BOTTOM_EXIT_ROW),\n    };\n    const exits = enemy.exitId === "right" ? [rightExit, bottomExit] : [bottomExit, rightExit];\n    const blocked = this.getBlockedPathCells();\n    const approximateRow = Phaser.Math.Clamp(Math.round((enemy.body.y - GRID_Y) / CELL), 0, GRID_ROWS - 1);\n    const approximateCol = Phaser.Math.Clamp(Math.round((enemy.body.x - GRID_X) / CELL), 0, GRID_COLS - 1);\n    const candidates: { col: number; row: number; x: number; y: number; distance: number }[] = [];\n\n    // Find nearby grid cells that the enemy can physically reach from its exact\n    // current position without crossing another plant. If a plant has just been\n    // placed directly over the enemy, that containing plant is ignored only for\n    // the short escape segment so the enemy can walk out instead of freezing.\n    for (let radius = 0; radius <= 4; radius += 1) {\n      for (let rowOffset = -radius; rowOffset <= radius; rowOffset += 1) {\n        for (let colOffset = -radius; colOffset <= radius; colOffset += 1) {\n          if (Math.max(Math.abs(colOffset), Math.abs(rowOffset)) !== radius) continue;\n          const col = approximateCol + colOffset;\n          const row = approximateRow + rowOffset;\n          if (col < 0 || col >= GRID_COLS || row < 0 || row >= GRID_ROWS) continue;\n          if (blocked.has(\`\${col},\${row}\`)) continue;\n          const x = this.gridToWorldX(col, row);\n          const y = this.gridToWorldY(row);\n          const movement = new Phaser.Geom.Line(enemy.body.x, enemy.body.y, x, y);\n          const segmentBlocked = this.towers.some((tower) => {\n            const currentInside = Math.abs(enemy.body.x - tower.body.x) < halfPlant\n              && Math.abs(enemy.body.y - tower.body.y) < halfPlant;\n            if (currentInside) return false;\n            const rect = new Phaser.Geom.Rectangle(\n              tower.body.x - halfPlant,\n              tower.body.y - halfPlant,\n              halfPlant * 2,\n              halfPlant * 2,\n            );\n            return Phaser.Geom.Intersects.LineToRectangle(movement, rect);\n          });\n          if (segmentBlocked) continue;\n          candidates.push({\n            col, row, x, y,\n            distance: Phaser.Math.Distance.Squared(enemy.body.x, enemy.body.y, x, y),\n          });\n        }\n      }\n      if (candidates.length > 0) break;\n    }\n\n    candidates.sort((a, b) => a.distance - b.distance);\n    for (const exit of exits) {\n      for (const candidate of candidates) {\n        const route = this.calculatePath(\n          { col: candidate.col, row: candidate.row },\n          { col: exit.col, row: exit.row },\n        );\n        if (!route) continue;\n\n        // Keep the exact current position as the first point. No setPosition()\n        // occurs here: rerouting never rewinds or teleports an enemy.\n        const routeTail = route.length > 0 ? route.slice(1) : [];\n        enemy.exitId = exit.id;\n        enemy.exitCol = exit.col;\n        enemy.exitRow = exit.row;\n        enemy.exitX = exit.x;\n        enemy.exitY = exit.y;\n        enemy.path = [\n          new Phaser.Math.Vector2(enemy.body.x, enemy.body.y),\n          new Phaser.Math.Vector2(candidate.x, candidate.y),\n          ...routeTail,\n          new Phaser.Math.Vector2(exit.x, exit.y),\n        ];\n        enemy.pathIndex = 1;\n        this.pendingEnemyReroutes.delete(enemy);\n        return true;\n      }\n    }\n\n    return false;\n  }\n\n  private followPath(enemy: Enemy, delta: number, speed: number): void {\n    const target = enemy.path[enemy.pathIndex];\n    if (!target) {\n      // Never let the legacy recalculation replace a live enemy path with [].\n      // A safe reroute either gives it a complete route or leaves its state intact.\n      this.rerouteEnemySafely(enemy);\n      return;\n    }\n\n    const distance = Phaser.Math.Distance.Between(enemy.body.x, enemy.body.y, target.x, target.y);\n    if (distance <= 0.001) {\n      enemy.body.setPosition(target.x, target.y);\n      enemy.pathIndex += 1;\n      if (this.pendingEnemyReroutes.has(enemy)) {\n        // We are naturally on a waypoint. Try the new route, but if no route is\n        // available yet keep the existing path; the movement guard below will\n        // prevent any illegal segment on the next frame.\n        if (!this.rerouteEnemySafely(enemy)) this.pendingEnemyReroutes.delete(enemy);\n      }\n      return;\n    }\n\n    const step = Math.min(distance, speed * (delta / 1000));\n    const nextX = enemy.body.x + ((target.x - enemy.body.x) / distance) * step;\n    const nextY = enemy.body.y + ((target.y - enemy.body.y) / distance) * step;\n    const halfPlant = PLANT_FRAME_SIZE / 2 - 1;\n    const movement = new Phaser.Geom.Line(enemy.body.x, enemy.body.y, nextX, nextY);\n\n    const movementBlocked = this.towers.some((tower) => {\n      const rect = new Phaser.Geom.Rectangle(\n        tower.body.x - halfPlant,\n        tower.body.y - halfPlant,\n        halfPlant * 2,\n        halfPlant * 2,\n      );\n      const currentInside = Math.abs(enemy.body.x - tower.body.x) < halfPlant\n        && Math.abs(enemy.body.y - tower.body.y) < halfPlant;\n      const nextInside = Math.abs(nextX - tower.body.x) < halfPlant\n        && Math.abs(nextY - tower.body.y) < halfPlant;\n      // If a plant was placed over an enemy, allow only movement that gets it\n      // farther from that plant's centre so it can escape instead of deadlocking.\n      if (currentInside) {\n        const currentDistance = Phaser.Math.Distance.Squared(enemy.body.x, enemy.body.y, tower.body.x, tower.body.y);\n        const nextDistance = Phaser.Math.Distance.Squared(nextX, nextY, tower.body.x, tower.body.y);\n        return nextInside && nextDistance <= currentDistance;\n      }\n      return nextInside || Phaser.Geom.Intersects.LineToRectangle(movement, rect);\n    });\n\n    if (movementBlocked) {\n      // Do not move through the plant and do not enter a permanent pending loop.\n      // A successful reroute starts from the enemy's real position. If none is\n      // found this frame, leave the enemy untouched and retry safely next frame.\n      this.rerouteEnemySafely(enemy);\n      return;\n    }\n\n    enemy.body.setPosition(nextX, nextY);\n    if (step >= distance - 0.001) {\n      enemy.body.setPosition(target.x, target.y);\n      enemy.pathIndex += 1;\n      if (this.pendingEnemyReroutes.has(enemy)) {\n        if (!this.rerouteEnemySafely(enemy)) this.pendingEnemyReroutes.delete(enemy);\n      }\n    }\n  }`;
      if (!code.includes(followPathAnchor)) {
        throw new Error("Deferred reroute followPath anchor not found.");
      }

      const recalculateAllAnchor = `  private recalculateEnemyPaths(): void {\n    const version = ++this.pathRecalculationVersion;\n    const activeEnemies = [...this.enemies];\n    const enemiesPerFrame = 6;\n    activeEnemies.forEach((enemy, index) => {\n      const delay = 1 + Math.floor(index / enemiesPerFrame) * 16;\n      const recalculate = (): void => {\n        if (version !== this.pathRecalculationVersion || !enemy.body.active || !this.enemies.includes(enemy)) return;\n        this.recalculateEnemyPath(enemy);\n      };\n      this.time.delayedCall(delay, recalculate);\n    });\n  }`;

      const recalculateAllReplacement = `  private recalculateEnemyPaths(): void {\n    // Placement/removal requests a reroute, but enemies continue naturally until\n    // a safe replacement route is available. No immediate teleport/recalculation.\n    this.pathRecalculationVersion += 1;\n    this.enemies.forEach((enemy) => {\n      if (enemy.body.active) this.pendingEnemyReroutes.add(enemy);\n    });\n  }`;
      if (!code.includes(recalculateAllAnchor)) {
        throw new Error("Deferred reroute batch anchor not found.");
      }

      let transformed = code.replace(fieldAnchor, fieldReplacement);
      transformed = transformed.replace(followPathAnchor, followPathReplacement);
      transformed = transformed.replace(recalculateAllAnchor, recalculateAllReplacement);
      return { code: transformed, map: null };
    },
  };
}
